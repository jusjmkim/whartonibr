<% unless post.image.url == '/images/original/missing.png' %>

  <% if image_type == 'scale' %>
    <%= image_tag post.image.url(:scale), class: 'column-image' %>
  <% elsif image_type == 'modal' %>
    <%= image_tag post.image.url(:modal), class: 'column-image' %>
  <% end %>

<% end %>