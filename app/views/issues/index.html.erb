<% content_for :head do %>
  <%= stylesheet_link_tag 'issues' %>
<% end %>

<div class="container">
  <img src="<%= image_path "IBR_logo.png" %>" class="logo">
  <div class="row">
    <% @issues.each_with_index do |issue, index| %>
      <a href="#" data-reveal-id="issue-<%= issue.id %>-modal" class='issue'>
        <div class="large-4 columns issue-container">
          <img src="<%= image_path issue.issue_img %>" class="issue-image">
          <span class="issue-name"><%= issue.name %></span>
        </div>
      </a>
      <% if (index + 1) % 3 == 0 %>
        </div><div class="row">
      <% end %>

      <%= render 'issues/issue_modal', issue: issue %>
    <% end %>
  </div>
</div>

<br />
<br />
<br />